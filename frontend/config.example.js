/**
 * config.example.js — Runtime API base URL configuration
 *
 * SETUP INSTRUCTIONS:
 *   1. Copy this file: cp config.example.js config.js
 *   2. Edit config.js with the correct URL for your environment:
 *        - Local dev  → http://localhost:8000
 *        - Production → https://your-app.onrender.com
 *
 * config.js is gitignored, so it is safe to put real URLs there.
 * Never commit config.js — each environment manages its own copy.
 */
window.API_BASE_URL = "http://localhost:8000";
